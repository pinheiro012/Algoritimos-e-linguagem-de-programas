int[] y = new int[10];
            int[] x = new int[10];
            for (int i = 0; i < 10; i++)
            {
                Console.WriteLine("Digite um número: ");
                x[i] = int.Parse(Console.ReadLine());
            }
            for (int i = 0; i < 10; i++)
            {
                y[i] = x[i] * x[i];
                Console.WriteLine("Vetor 1: {0}\nVetor 2: {1}\n\n", x[i], y[i]);
